fastapi
uvicorn
aiokafka 
websockets
python-multipart
sqlalchemy
psycopg2-binary
python-jose[cryptography]
bcrypt
email-validator
# forcing win32 selector for aiokafka on windows if needed usually handled in code, 
# but dependencies are standard.

# Optional: Uncomment for Cloudflare R2 storage support
# boto3
